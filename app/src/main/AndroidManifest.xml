<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Signup"
        tools:targetApi="31">
        <activity
            android:name=".sa.searchcontact"
            android:exported="false" />
        <activity
            android:name=".data.addcontact"
            android:exported="false" />
        <activity
            android:name=".sa.contact"
            android:exported="false" />
        <activity
            android:name=".welcomescreen"
            android:exported="false" />
        <activity
            android:name=".login.signIn"
            android:exported="false" />
        <activity
            android:name=".login.sign"
            android:exported="false"
            android:label="@string/title_activity_sign"
            android:theme="@style/Theme.Signup.NoActionBar" />
        <activity
            android:name=".login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>